import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';
// reportWebVitals removed for Week 1 minimal submission

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

// performance reporting removed for minimal Week 1 submission
