# Frontend — Medilink React App

Requirements: Node 18+ and npm

Quick start (from repo root):

```bash
cd frontend
npm install
npm run dev
```

The dev server runs on `http://localhost:5173` by default. The frontend talks to the backend API at `http://localhost:4000`.
