# Week 1 — Repo setup & Hello World

This folder points to the Week 1 artifacts in the repository.

Backend (Md. Mostakim Ahmed Sakib)
- Repo: `backend/` (`backend/package.json`)
- Install packages & DB: `backend/migrate.js` (migration script)
- Hello-world API: `backend/index.js` (GET `/`)

Frontend (Md. Saiful Islam)
- Repo: `frontend/` (`frontend/package.json`)
- Hello-world page: `frontend/src/App.js` and `frontend/src/pages/*`

How to run (quick):
```bash
cd backend
npm install
node migrate.js
node index.js

cd ../frontend
npm install
npm start
```
